# ملف .gitignore احترافي لمشاريع Python/Discord bots

### ━━━━━━━━━━━━━━━━━━━━
### 1. ملفات نظام/IDE
### ━━━━━━━━━━━━━━━━━━━━
.DS_Store           # ملفات نظام macOS
.idea/              # إعدادات PyCharm/IntelliJ
.vscode/            # إعدادات VS Code (استثنِ إذا كان لديك إعدادات مهمة)
.trae/
*.swp               # ملفات Vim المؤقتة
*.swo
Thumbs.db           # ملفات Windows

### ━━━━━━━━━━━━━━━━━━━━
### 2. بيئة Python
### ━━━━━━━━━━━━━━━━━━━━
__pycache__/        # بايت كود Python المترجم
__pycache__
*.py[cod]           
*.so                
.Python             
env/                # مجلد البيئة الافتراضية
venv/               # مجلدات البيئات الافتراضية
.env                # ملفات البيئة (يجب استثناؤها إلا إذا كانت للتنمية المحلية فقط)
.venv/
ENV/
env.bak/
venv.bak/
pip-selfcheck.json

### ━━━━━━━━━━━━━━━━━━━━
### 3. ملفات التثبيت والتبعيات
### ━━━━━━━━━━━━━━━━━━━━
*.egg-info/         # معلومات حزم Python
dist/               # حزم التوزيع
build/              
*.egg               
*.wheel             
.cache/             # ذاكرة pip/أدوات أخرى

### ━━━━━━━━━━━━━━━━━━━━
### 4. ملفات الإعدادات والبيانات الحساسة
### ━━━━━━━━━━━━━━━━━━━━
config.py           # ملفات الإعدادات (يجب استخدام .env بدلاً منها)
config.json         
secrets.json        
*.key               
*.pem               
*.crt               
credentials*        
*.env               # ملفات البيئة (إلا إذا كانت للتنمية فقط)
.env.local         
.env.*.local       
security_bot.db

### ━━━━━━━━━━━━━━━━━━━━
### 5. ملفات السجلات والبيانات الديناميكية
### ━━━━━━━━━━━━━━━━━━━━
*.log               # ملفات السجلات
logs/               
data/               # بيانات التطبيق (إلا إذا كانت ضرورية للمشاركة)
*.db                # قواعد بيانات محددة
security_bot.db      # قاعدة بيانات البوت

# ملفات البيانات المؤقتة
data/temp/
data/cache/
.cache/             # ذاكرة pip/أدوات أخرى

### ━━━━━━━━━━━━━━━━━━━━
### 4. ملفات الإعدادات والبيانات الحساسة
### ━━━━━━━━━━━━━━━━━━━━
config.py           # ملفات الإعدادات (يجب استخدام .env بدلاً منها)
config.json         
secrets.json        
*.key               
*.pem               
*.crt               
credentials*        
*.env               # ملفات البيئة (إلا إذا كانت للتنمية فقط)
.env.local         
.env.*.local       
.env

### ━━━━━━━━━━━━━━━━━━━━
### 5. ملفات السجلات والبيانات الديناميكية
### ━━━━━━━━━━━━━━━━━━━━
*.log               # ملفات السجلات
logs/               
data/               # بيانات التطبيق (إلا إذا كانت ضرورية للمشاركة)
*.db                # قواعد بيانات محلية
*.sqlite            
*.sqlite3           

### ━━━━━━━━━━━━━━━━━━━━
### 6. ملفات النظام/التثبيت
### ━━━━━━━━━━━━━━━━━━━━
*.exe               # الملفات القابلة للتنفيذ
*.dll               
*.bin               
*.msi               

### ━━━━━━━━━━━━━━━━━━━━
### 7. ملفات خاصة بـ Jupyter/Notebooks
### ━━━━━━━━━━━━━━━━━━━━
.ipynb_checkpoints/ 
*.ipynb             # (استثنِ إذا كان المشروع يعتمد على Notebooks)

### ━━━━━━━━━━━━━━━━━━━━
### 8. ملفات التغطية والاختبارات
### ━━━━━━━━━━━━━━━━━━━━
.coverage           # تقارير تغطية الكود
htmlcov/            
.pytest_cache/      
test_output/        

### ━━━━━━━━━━━━━━━━━━━━
### 9. ملفات نظام التحكم بالإصدارات
### ━━━━━━━━━━━━━━━━━━━━
.git/               # (نادرًا ما يضاف، لكنه احتياطي)
*.orig              # ملفات دمج Git المؤقتة
*.rej               

### ━━━━━━━━━━━━━━━━━━━━
### 10. ملفات خاصة بالسيستم
### ━━━━━━━━━━━━━━━━━━━━
instance/           # مجلد Flask instance (إن وجد)
.webassets-cache/   # إذا كنت تستخدم Flask-Webassets
.sass-cache/        # إذا كنت تستخدم SASS